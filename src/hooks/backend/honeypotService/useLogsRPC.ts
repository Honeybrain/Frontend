import React from "react";
import

const useLogsRPC = () => {
    const streamLogs = React.useCallback(
        async () => {


        },
        []
    );

    return streamLogs;
};

export default useLogsRPC;
